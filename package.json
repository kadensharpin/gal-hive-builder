{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "gh-pages": "rm -rf dist/ && vite build && git checkout gh-pages && rm -rf assets/ && cp -r dist/* . && git add . && git commit -m \"Update gh-pages\" && git checkout main"
  },
  "devDependencies": {
    "vite": "^6.0.10"
  }
}
